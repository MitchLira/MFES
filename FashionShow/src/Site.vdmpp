class Site
	types
	-- TODO Define types here
	instance variables
		users: set of User;
		inv not exists u1, u2 in set users & u1 <> u2 and u1.username = u2.username;
		shows: set of FashionShow;
	
	operations
	public Site:() ==> Site 
		Site() == 
		(
			users := {};
			shows := {};
			return self;
		);
		
	functions
	
	traces
	-- TODO Define Combinatorial Test Traces here
end Site