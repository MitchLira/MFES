class Item
types
public Ref =  seq of char 
	inv v == len v = 9;
	public Size = <XS>|<S>|<M>|<L>|<XL>|<XXL>;

instance variables
	public name: Plataform`String;
	public reference: Ref; --TODO p�r reference unica
	public price: Plataform`Price;
	public size: Size;

operations
	public Item: Plataform`String * Ref * Plataform`Price * Size ==> Item 
		Item(n, r, p, s) == 
		(
			name := n;
			reference := r;
			price := p;
			size := s;
			return self;
		);
		
	public getName: () ==> Plataform`String
		getName() == return name
		post RESULT = name;
	
	public getSize: () ==> Size
		getSize() == return size
		post RESULT = size;
		
	public getReference: () ==> Ref
		getReference() == return reference
		post RESULT = reference;
	
	public getPrice: () ==> Plataform`Price
		getPrice() == return price
		post RESULT = price;
functions
	-- TODO Define functiones here
end Item